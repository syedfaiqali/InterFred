# Vite + React + TypeScript + Tailwind + Redux (minimal)

Install and run:

```bash
npm install
npm run dev
```

Build:

```bash
npm run build
npm run preview
```

This project uses React functional components, lazy loading with `React.lazy` and `Suspense`, and Redux Toolkit for state.
